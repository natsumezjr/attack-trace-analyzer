# Backend infrastructure environment variables (OpenSearch + Dashboards + Neo4j)
#
# Usage:
#   cd backend
#   cp .env.example .env
#   docker compose up -d
#
# Notes:
# - This file is consumed by `backend/docker-compose.yml` via `env_file`.
# - The FastAPI backend (running via `uv run uvicorn ...`) does NOT automatically load this file.
#   If you want to reuse these values for the backend process, explicitly export them in your shell.

# -------- OpenSearch --------
OPENSEARCH_INITIAL_ADMIN_PASSWORD=OpenSearch@2024!Dev
# Keep this small on laptops; increase on servers.
OPENSEARCH_JAVA_OPTS=-Xms512m -Xmx512m

# -------- Neo4j --------
NEO4J_AUTH=neo4j/password
NEO4J_server_memory_pagecache_size=1G
NEO4J_server_memory_heap_initial__size=1G
NEO4J_server_memory_heap_max__size=1G

# -------- Optional: LLM config (only used by specific analysis modules) --------
# For local/offline development, keep `mock`. To use DeepSeek, set `deepseek` + `DEEPSEEK_API_KEY`.
LLM_PROVIDER=mock
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-chat
LLM_TIMEOUT=30.0
LLM_MAX_RETRIES=2

