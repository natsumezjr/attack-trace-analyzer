# Copy to `.env` on each client node and customize.
#
# Notes:
# - This compose is intended to run on the *Linux client machines* in your range (靶机).
# - Center will poll the client backend over the internal network.

# -------- Node identity (must be stable) --------
CLIENT_ID=client-victim-01
HOST_ID=h-a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6
HOST_NAME=victim-01

# -------- Center connectivity --------
# Center backend register endpoint (Next.js)
CENTER_BASE_URL=http://10.0.0.1:3000

# The URL that center can use to reach this node's client backend (no NAT/port mapping in your setup).
CLIENT_LISTEN_URL=http://10.0.0.11:18080

# -------- Client backend runtime --------
CLIENT_BIND_ADDR=0.0.0.0:18080
CLIENT_TIMEZONE=UTC

# -------- Sensor input paths (host paths) --------
# Filebeat ECS ndjson (recommended: Filebeat + Sigma detector output, one JSON object per line)
# Example (compose volume): /data/filebeat-output/ecs_logs_with_anomalies.json
ATA_FILEBEAT_EVENTS_JSON=/var/lib/ata/filebeat/ecs_logs_with_anomalies.json

# -------- Suricata capture config --------
# The interface name on the Linux client node (e.g. eth0/ens33/enp0s3)
SURICATA_INTERFACE=eth0
